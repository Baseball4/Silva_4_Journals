1. int = integer,i.e whole or "counting" numbers, e.g. 3, 5, -6.
2. float = number with decimal, e.g. 3.14, 32-bit.
3. short, long, double = 16-bit, 64-bit, double we will use for decimals.
4. String = is for a line of words to print.
5. char = a single 16-bit Unicode character.
